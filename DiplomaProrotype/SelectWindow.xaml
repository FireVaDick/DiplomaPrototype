<Window x:Class="DiplomaPrototype.SelectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiplomaPrototype"
        mc:Ignorable="d"      
        Title="Окно выбора" Height="145" Width="350" ResizeMode="NoResize">
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type Label}">
                <Setter Property="Width" Value="170" />
            </Style>
            <Style TargetType="{x:Type TextBox}">
                <Setter Property="Width" Value="130" />
            </Style>
            <Style TargetType="{x:Type Button}">
                <Setter Property="Width" Value="155" />
                <Setter Property="Height" Value="25" />
                <Setter Property="Margin" Value="10" />
            </Style>
        </Grid.Resources>

        <Label Name="NumberLabel" Margin="10, 10, 5, 5" HorizontalAlignment="Left" VerticalAlignment="Top">1</Label>
        <TextBox Name="NumberTextBox" Margin="10, 15, 15, 10" HorizontalAlignment="Right" VerticalAlignment="Top" />

        <Label Name="PlacesLabel" Margin="10, 35, 5, 5" HorizontalAlignment="Left" VerticalAlignment="Top">2</Label>
        <TextBox Name="PlacesTextBox" Margin="10, 40, 15, 10" HorizontalAlignment="Right" VerticalAlignment="Top" />

        <Button Name="OkButton" 
                Click="OkButton_Click" 
                IsDefault="True" 
                HorizontalAlignment="Left" 
                VerticalAlignment="Bottom" 
                Cursor="Hand">
            <Button.Background>
                <LinearGradientBrush StartPoint="0.0, 0.0" EndPoint="0.9, 1" >
                    <GradientStop Color="#84A98C" Offset="0"/>
                    <GradientStop Color="#E5E9EC" Offset="0.8"/>
                </LinearGradientBrush>
            </Button.Background> Подтвердить</Button>
        <Button Name="CancelButton" 
                Click="CancelButton_Click" 
                IsCancel="True" 
                HorizontalAlignment="Right" 
                VerticalAlignment="Bottom" 
                Cursor="Hand">
            <Button.Background>
                <LinearGradientBrush StartPoint="0.0, 0.0" EndPoint="0.9, 1" >
                    <GradientStop Color="#E5E9EC" Offset="0.2"/>
                    <GradientStop Color="#84A98C" Offset="1"/>
                </LinearGradientBrush>
            </Button.Background> Отменить</Button>
    </Grid>
</Window>
